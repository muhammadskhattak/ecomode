import React from 'react';
import EcomodeNav from '../GlobalComponents/EcomodeNav';

const signup = (props) => {
    return (
        <div>
            <EcomodeNav />
            <p>Sign up info here!</p>
        </div>
    );
};

export default signup;