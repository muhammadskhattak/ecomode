import React from 'react';

const tickets = (props) => {
    return (
        <div>
            <p>This is where you can get information on how to buy ticekts!</p>
        </div>
    );
}

export default tickets;